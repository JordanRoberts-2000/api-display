AuthSessionErrorResponse:
  type: object
  required:
    - error
  properties:
    error:
      $ref: "#/AuthSessionErrorCode"

AuthSessionErrorCode:
  type: string
  enum:
    - no_session
    - session_expired
