openapi: 3.1.0

info:
  title: API Display API
  version: 0.1.0
  description: API contract for the API Display app.

paths:
  /health:
    $ref: "./paths/health.yaml#/health"

  /api/auth/session:
    $ref: "./paths/api/auth.yaml#/session"

components:
  schemas:
    AuthSessionErrorResponse:
      $ref: "./components/schemas/auth.yaml#/AuthSessionErrorResponse"

    AuthSessionErrorCode:
      $ref: "./components/schemas/auth.yaml#/AuthSessionErrorCode"

    HealthResponse:
      $ref: "./components/schemas/health.yaml#/HealthResponse"
